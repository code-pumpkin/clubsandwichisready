import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const StudentProgress: React.FC = () => {
  return (
    <Box p={8}>
      <Heading mb={6}>Student Progress</Heading>
      {/* Add student progress functionality here */}
    </Box>
  );
};

export default StudentProgress;