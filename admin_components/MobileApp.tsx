import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const MobileApp: React.FC = () => {
  return (
    <Box p={8}>
      <Heading mb={6}>Mobile App</Heading>
      {/* Add mobile app functionality here */}
    </Box>
  );
};

export default MobileApp;